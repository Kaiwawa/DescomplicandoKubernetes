apiVersion: v1
kind: Pod
metadata:
  labels:
    run: giropos
  name: giropos-5
spec:
  containers:
  - image: nginx
    name: giropos
    ports:
    - containerPort: 80
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
